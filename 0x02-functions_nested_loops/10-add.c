#include "holberton.h"

/**
 * add - returns the sum of two numbers
 * @a: one of the numbers to add
 * @b: the other number to add
 *
 * Return: the sum of a and b
 */
int add(int a, int b)
{
	return (a + b);
}
