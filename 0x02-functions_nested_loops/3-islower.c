/**
 * _islower - checks whether a letter is lower case
 * @c: the character to check
 *
 * Return: 1 if c is a letter and is lower case, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
