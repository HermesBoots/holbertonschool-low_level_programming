#include "holberton.h"

/**
 * _abs - return the absolute value of a number
 * @n: the number to compute the absolute value from
 *
 * Return: the absolute value
 */
int _abs(int n)
{
	return (n > 0 ? n : -n);
}
